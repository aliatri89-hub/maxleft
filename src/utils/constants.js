// ─── STATIC DATA & CONSTANTS ─────────────────────────────────

export const DEFAULT_ENABLED_SHELVES = { books: true, movies: true, shows: true, games: true };
export const DEFAULT_SHELF_ORDER = ["books", "movies", "shows", "games"];
